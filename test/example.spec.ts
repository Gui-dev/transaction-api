import { describe, expect, it } from 'vitest'

describe('', () => {
  it('should be able to user create a transaction', async () => {
    const responseStatusHttp = 201

    expect(responseStatusHttp).toEqual(201)
  })
})
